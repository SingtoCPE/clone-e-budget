export enum PathName {
  Dashboard = "dashboard",
  MasterData = "master-data",
  Form = "form",
}

export enum MasterDataPathName {
  Activities = "activities",
  AreaBased = "area-based",
  BudgetCategories = "budget-categories",
  NationalStrategies = "national-strategies",
  Organizations = "organizations",
  OutputProjects = "output-projects",
  Plans = "plans",
  RiverBasins = "river-basins",
  RoyalInitiativeTypes = "royal-initiative-types",
}

export enum FormPathName {
  ExpenseBudgetDraft = "expense-budget-draft",
  GovPersonnelExpense= 'government-personnel-expense'
}

export enum Method {
  Create = "create",
  Edit = "edit",
  Delete = "delete",
}

